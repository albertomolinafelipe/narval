type: object
required:
  - available
properties:
  available:
    type: boolean
  reason:
    type: string
    enum:
      - subdomain
    description: Why the website was rejected (only present when unavailable).
