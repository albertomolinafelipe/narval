// This file is auto-generated by @hey-api/openapi-ts

import {
  type DefaultError,
  queryOptions,
  type UseMutationOptions,
} from "@tanstack/react-query";

import { client } from "../client.gen";
import {
  boostStartup,
  checkStartupWebsite,
  confirmDomainVerification,
  confirmInstagramVerification,
  createAdminStartup,
  createStartup,
  deleteStartupBanner,
  deleteStartupLogo,
  favoriteStartup,
  getClaimLink,
  getClaimStartup,
  getHealth,
  getInstagramVerification,
  getMe,
  getStartup,
  getStats,
  listInstagramVerifications,
  listStartups,
  login,
  logout,
  type Options,
  register,
  resetInstagramVerification,
  startClaim,
  startDomainVerification,
  startInstagramVerification,
  unfavoriteStartup,
  updateStartup,
  uploadFounderPhoto,
  uploadStartupBanner,
  uploadStartupLogo,
  uploadStartupScreenshot,
  verify,
} from "../sdk.gen";
import type {
  BoostStartupData,
  BoostStartupError,
  CheckStartupWebsiteData,
  CheckStartupWebsiteResponse,
  ConfirmDomainVerificationData,
  ConfirmDomainVerificationError,
  ConfirmDomainVerificationResponse,
  ConfirmInstagramVerificationData,
  ConfirmInstagramVerificationError,
  ConfirmInstagramVerificationResponse,
  CreateAdminStartupData,
  CreateAdminStartupError,
  CreateAdminStartupResponse2,
  CreateStartupData,
  CreateStartupError,
  CreateStartupResponse,
  DeleteStartupBannerData,
  DeleteStartupBannerError,
  DeleteStartupBannerResponse,
  DeleteStartupLogoData,
  DeleteStartupLogoError,
  DeleteStartupLogoResponse,
  FavoriteStartupData,
  FavoriteStartupError,
  GetClaimLinkData,
  GetClaimLinkError,
  GetClaimLinkResponse,
  GetClaimStartupData,
  GetClaimStartupError,
  GetClaimStartupResponse,
  GetHealthData,
  GetHealthResponse,
  GetInstagramVerificationData,
  GetInstagramVerificationError,
  GetInstagramVerificationResponse,
  GetMeData,
  GetMeError,
  GetMeResponse,
  GetStartupData,
  GetStartupError,
  GetStartupResponse,
  GetStatsData,
  GetStatsResponse,
  ListInstagramVerificationsData,
  ListInstagramVerificationsError,
  ListInstagramVerificationsResponse,
  ListStartupsData,
  ListStartupsResponse,
  LoginData,
  LoginError,
  LogoutData,
  RegisterData,
  RegisterError,
  ResetInstagramVerificationData,
  ResetInstagramVerificationError,
  ResetInstagramVerificationResponse,
  StartClaimData,
  StartClaimError,
  StartDomainVerificationData,
  StartDomainVerificationError,
  StartDomainVerificationResponse,
  StartInstagramVerificationData,
  StartInstagramVerificationError,
  StartInstagramVerificationResponse,
  UnfavoriteStartupData,
  UnfavoriteStartupError,
  UnfavoriteStartupResponse,
  UpdateStartupData,
  UpdateStartupError,
  UpdateStartupResponse,
  UploadFounderPhotoData,
  UploadFounderPhotoError,
  UploadFounderPhotoResponse,
  UploadStartupBannerData,
  UploadStartupBannerError,
  UploadStartupBannerResponse,
  UploadStartupLogoData,
  UploadStartupLogoError,
  UploadStartupLogoResponse,
  UploadStartupScreenshotData,
  UploadStartupScreenshotError,
  UploadStartupScreenshotResponse,
  VerifyData,
  VerifyError,
  VerifyResponse,
} from "../types.gen";

export type QueryKey<TOptions extends Options> = [
  Pick<TOptions, "baseUrl" | "body" | "headers" | "path" | "query"> & {
    _id: string;
    _infinite?: boolean;
    tags?: ReadonlyArray<string>;
  },
];

const createQueryKey = <TOptions extends Options>(
  id: string,
  options?: TOptions,
  infinite?: boolean,
  tags?: ReadonlyArray<string>,
): [QueryKey<TOptions>[0]] => {
  const params: QueryKey<TOptions>[0] = {
    _id: id,
    baseUrl:
      options?.baseUrl || (options?.client ?? client).getConfig().baseUrl,
  } as QueryKey<TOptions>[0];
  if (infinite) {
    params._infinite = infinite;
  }
  if (tags) {
    params.tags = tags;
  }
  if (options?.body) {
    params.body = options.body;
  }
  if (options?.headers) {
    params.headers = options.headers;
  }
  if (options?.path) {
    params.path = options.path;
  }
  if (options?.query) {
    params.query = options.query;
  }
  return [params];
};

export const getHealthQueryKey = (options?: Options<GetHealthData>) =>
  createQueryKey("getHealth", options);

/**
 * Health check
 */
export const getHealthOptions = (options?: Options<GetHealthData>) =>
  queryOptions<
    GetHealthResponse,
    DefaultError,
    GetHealthResponse,
    ReturnType<typeof getHealthQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await getHealth({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getHealthQueryKey(options),
  });

export const getStatsQueryKey = (options?: Options<GetStatsData>) =>
  createQueryKey("getStats", options);

/**
 * Aggregate directory counts (startups + users)
 */
export const getStatsOptions = (options?: Options<GetStatsData>) =>
  queryOptions<
    GetStatsResponse,
    DefaultError,
    GetStatsResponse,
    ReturnType<typeof getStatsQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await getStats({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getStatsQueryKey(options),
  });

/**
 * Begin passwordless login — sends a verification code
 */
export const loginMutation = (
  options?: Partial<Options<LoginData>>,
): UseMutationOptions<unknown, LoginError, Options<LoginData>> => {
  const mutationOptions: UseMutationOptions<
    unknown,
    LoginError,
    Options<LoginData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await login({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * Begin registration for all account types — sends a verification code
 *
 * Unified registration endpoint. For account_type="user", provide email and nickname. For account_type="startup", provide name and email. Domain verification happens later from the profile, not at signup.
 */
export const registerMutation = (
  options?: Partial<Options<RegisterData>>,
): UseMutationOptions<unknown, RegisterError, Options<RegisterData>> => {
  const mutationOptions: UseMutationOptions<
    unknown,
    RegisterError,
    Options<RegisterData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await register({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * Verify the emailed code and create the account and session
 *
 * Consumes the OTP for registration, login, and claim flows alike. On success a SuperTokens session cookie is set.
 */
export const verifyMutation = (
  options?: Partial<Options<VerifyData>>,
): UseMutationOptions<VerifyResponse, VerifyError, Options<VerifyData>> => {
  const mutationOptions: UseMutationOptions<
    VerifyResponse,
    VerifyError,
    Options<VerifyData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await verify({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * Logout (revoke the current session)
 */
export const logoutMutation = (
  options?: Partial<Options<LogoutData>>,
): UseMutationOptions<unknown, DefaultError, Options<LogoutData>> => {
  const mutationOptions: UseMutationOptions<
    unknown,
    DefaultError,
    Options<LogoutData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await logout({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

export const getMeQueryKey = (options?: Options<GetMeData>) =>
  createQueryKey("getMe", options);

/**
 * Get current authenticated user's profile
 */
export const getMeOptions = (options?: Options<GetMeData>) =>
  queryOptions<
    GetMeResponse,
    GetMeError,
    GetMeResponse,
    ReturnType<typeof getMeQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await getMe({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getMeQueryKey(options),
  });

/**
 * Begin claiming an admin-seeded shell — sends a verification code
 *
 * Validates the claim token and emails an OTP to the given address. /auth/verify completes the claim and reassigns ownership of the shell. Works for brand-new emails and existing accounts alike.
 */
export const startClaimMutation = (
  options?: Partial<Options<StartClaimData>>,
): UseMutationOptions<unknown, StartClaimError, Options<StartClaimData>> => {
  const mutationOptions: UseMutationOptions<
    unknown,
    StartClaimError,
    Options<StartClaimData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await startClaim({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

export const getClaimStartupQueryKey = (
  options: Options<GetClaimStartupData>,
) => createQueryKey("getClaimStartup", options);

/**
 * Fetch an unclaimed shell by its claim token (public)
 *
 * Used by the claim page to preview the profile. The token itself is the capability, so no session is required.
 */
export const getClaimStartupOptions = (options: Options<GetClaimStartupData>) =>
  queryOptions<
    GetClaimStartupResponse,
    GetClaimStartupError,
    GetClaimStartupResponse,
    ReturnType<typeof getClaimStartupQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await getClaimStartup({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getClaimStartupQueryKey(options),
  });

export const listStartupsQueryKey = (options?: Options<ListStartupsData>) =>
  createQueryKey("listStartups", options);

/**
 * List all startup accounts (public)
 */
export const listStartupsOptions = (options?: Options<ListStartupsData>) =>
  queryOptions<
    ListStartupsResponse,
    DefaultError,
    ListStartupsResponse,
    ReturnType<typeof listStartupsQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await listStartups({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: listStartupsQueryKey(options),
  });

/**
 * Create a new startup account
 */
export const createStartupMutation = (
  options?: Partial<Options<CreateStartupData>>,
): UseMutationOptions<
  CreateStartupResponse,
  CreateStartupError,
  Options<CreateStartupData>
> => {
  const mutationOptions: UseMutationOptions<
    CreateStartupResponse,
    CreateStartupError,
    Options<CreateStartupData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await createStartup({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

export const checkStartupWebsiteQueryKey = (
  options: Options<CheckStartupWebsiteData>,
) => createQueryKey("checkStartupWebsite", options);

/**
 * Check whether a website URL is already registered by a startup
 */
export const checkStartupWebsiteOptions = (
  options: Options<CheckStartupWebsiteData>,
) =>
  queryOptions<
    CheckStartupWebsiteResponse,
    DefaultError,
    CheckStartupWebsiteResponse,
    ReturnType<typeof checkStartupWebsiteQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await checkStartupWebsite({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: checkStartupWebsiteQueryKey(options),
  });

export const getStartupQueryKey = (options: Options<GetStartupData>) =>
  createQueryKey("getStartup", options);

/**
 * Get a startup by UUID or verified domain (public)
 */
export const getStartupOptions = (options: Options<GetStartupData>) =>
  queryOptions<
    GetStartupResponse,
    GetStartupError,
    GetStartupResponse,
    ReturnType<typeof getStartupQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await getStartup({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getStartupQueryKey(options),
  });

/**
 * Update the authenticated user's startup profile
 */
export const updateStartupMutation = (
  options?: Partial<Options<UpdateStartupData>>,
): UseMutationOptions<
  UpdateStartupResponse,
  UpdateStartupError,
  Options<UpdateStartupData>
> => {
  const mutationOptions: UseMutationOptions<
    UpdateStartupResponse,
    UpdateStartupError,
    Options<UpdateStartupData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await updateStartup({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * Remove the logo image for the startup
 */
export const deleteStartupLogoMutation = (
  options?: Partial<Options<DeleteStartupLogoData>>,
): UseMutationOptions<
  DeleteStartupLogoResponse,
  DeleteStartupLogoError,
  Options<DeleteStartupLogoData>
> => {
  const mutationOptions: UseMutationOptions<
    DeleteStartupLogoResponse,
    DeleteStartupLogoError,
    Options<DeleteStartupLogoData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await deleteStartupLogo({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * Upload a logo image for the startup
 */
export const uploadStartupLogoMutation = (
  options?: Partial<Options<UploadStartupLogoData>>,
): UseMutationOptions<
  UploadStartupLogoResponse,
  UploadStartupLogoError,
  Options<UploadStartupLogoData>
> => {
  const mutationOptions: UseMutationOptions<
    UploadStartupLogoResponse,
    UploadStartupLogoError,
    Options<UploadStartupLogoData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await uploadStartupLogo({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * Remove the banner image for the startup
 */
export const deleteStartupBannerMutation = (
  options?: Partial<Options<DeleteStartupBannerData>>,
): UseMutationOptions<
  DeleteStartupBannerResponse,
  DeleteStartupBannerError,
  Options<DeleteStartupBannerData>
> => {
  const mutationOptions: UseMutationOptions<
    DeleteStartupBannerResponse,
    DeleteStartupBannerError,
    Options<DeleteStartupBannerData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await deleteStartupBanner({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * Upload or replace the banner image for the startup
 */
export const uploadStartupBannerMutation = (
  options?: Partial<Options<UploadStartupBannerData>>,
): UseMutationOptions<
  UploadStartupBannerResponse,
  UploadStartupBannerError,
  Options<UploadStartupBannerData>
> => {
  const mutationOptions: UseMutationOptions<
    UploadStartupBannerResponse,
    UploadStartupBannerError,
    Options<UploadStartupBannerData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await uploadStartupBanner({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * Remove favorite from a startup for the authenticated user
 */
export const unfavoriteStartupMutation = (
  options?: Partial<Options<UnfavoriteStartupData>>,
): UseMutationOptions<
  UnfavoriteStartupResponse,
  UnfavoriteStartupError,
  Options<UnfavoriteStartupData>
> => {
  const mutationOptions: UseMutationOptions<
    UnfavoriteStartupResponse,
    UnfavoriteStartupError,
    Options<UnfavoriteStartupData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await unfavoriteStartup({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * Favorite a startup for the authenticated user
 */
export const favoriteStartupMutation = (
  options?: Partial<Options<FavoriteStartupData>>,
): UseMutationOptions<
  unknown,
  FavoriteStartupError,
  Options<FavoriteStartupData>
> => {
  const mutationOptions: UseMutationOptions<
    unknown,
    FavoriteStartupError,
    Options<FavoriteStartupData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await favoriteStartup({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * Boost a startup (irreversible, expires after 7 days)
 */
export const boostStartupMutation = (
  options?: Partial<Options<BoostStartupData>>,
): UseMutationOptions<
  unknown,
  BoostStartupError,
  Options<BoostStartupData>
> => {
  const mutationOptions: UseMutationOptions<
    unknown,
    BoostStartupError,
    Options<BoostStartupData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await boostStartup({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * Start domain verification for a startup — emails a one-time code
 */
export const startDomainVerificationMutation = (
  options?: Partial<Options<StartDomainVerificationData>>,
): UseMutationOptions<
  StartDomainVerificationResponse,
  StartDomainVerificationError,
  Options<StartDomainVerificationData>
> => {
  const mutationOptions: UseMutationOptions<
    StartDomainVerificationResponse,
    StartDomainVerificationError,
    Options<StartDomainVerificationData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await startDomainVerification({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * Confirm domain verification with the one-time code
 */
export const confirmDomainVerificationMutation = (
  options?: Partial<Options<ConfirmDomainVerificationData>>,
): UseMutationOptions<
  ConfirmDomainVerificationResponse,
  ConfirmDomainVerificationError,
  Options<ConfirmDomainVerificationData>
> => {
  const mutationOptions: UseMutationOptions<
    ConfirmDomainVerificationResponse,
    ConfirmDomainVerificationError,
    Options<ConfirmDomainVerificationData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await confirmDomainVerification({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

export const getInstagramVerificationQueryKey = (
  options: Options<GetInstagramVerificationData>,
) => createQueryKey("getInstagramVerification", options);

/**
 * Get the current Instagram verification challenge for a startup
 */
export const getInstagramVerificationOptions = (
  options: Options<GetInstagramVerificationData>,
) =>
  queryOptions<
    GetInstagramVerificationResponse,
    GetInstagramVerificationError,
    GetInstagramVerificationResponse,
    ReturnType<typeof getInstagramVerificationQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await getInstagramVerification({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getInstagramVerificationQueryKey(options),
  });

/**
 * Lock an Instagram handle and get the code to DM the company account
 */
export const startInstagramVerificationMutation = (
  options?: Partial<Options<StartInstagramVerificationData>>,
): UseMutationOptions<
  StartInstagramVerificationResponse,
  StartInstagramVerificationError,
  Options<StartInstagramVerificationData>
> => {
  const mutationOptions: UseMutationOptions<
    StartInstagramVerificationResponse,
    StartInstagramVerificationError,
    Options<StartInstagramVerificationData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await startInstagramVerification({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

export const getClaimLinkQueryKey = (options: Options<GetClaimLinkData>) =>
  createQueryKey("getClaimLink", options);

/**
 * Get the claim token for a startup shell (owner only)
 *
 * Lets the admin (re)copy the claim link from the edit page. The token is empty once the profile has been claimed.
 */
export const getClaimLinkOptions = (options: Options<GetClaimLinkData>) =>
  queryOptions<
    GetClaimLinkResponse,
    GetClaimLinkError,
    GetClaimLinkResponse,
    ReturnType<typeof getClaimLinkQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await getClaimLink({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getClaimLinkQueryKey(options),
  });

/**
 * Upload a founder photo and get back its public URL
 *
 * The caller stores the URL inside the startup's founders JSON; this endpoint does not modify the startup itself.
 */
export const uploadFounderPhotoMutation = (
  options?: Partial<Options<UploadFounderPhotoData>>,
): UseMutationOptions<
  UploadFounderPhotoResponse,
  UploadFounderPhotoError,
  Options<UploadFounderPhotoData>
> => {
  const mutationOptions: UseMutationOptions<
    UploadFounderPhotoResponse,
    UploadFounderPhotoError,
    Options<UploadFounderPhotoData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await uploadFounderPhoto({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * Upload a product screenshot and get back its public URL
 *
 * The caller stores the URL inside the startup's gallery JSON array; this endpoint does not modify the startup itself.
 */
export const uploadStartupScreenshotMutation = (
  options?: Partial<Options<UploadStartupScreenshotData>>,
): UseMutationOptions<
  UploadStartupScreenshotResponse,
  UploadStartupScreenshotError,
  Options<UploadStartupScreenshotData>
> => {
  const mutationOptions: UseMutationOptions<
    UploadStartupScreenshotResponse,
    UploadStartupScreenshotError,
    Options<UploadStartupScreenshotData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await uploadStartupScreenshot({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * Create an unclaimed startup shell (admin only)
 *
 * Seeds a shell profile owned by the admin. The admin fills it in via the normal edit page and hands the claim link to the startup. Skips the public one-per-owner / account-type / website rules — those only apply to real self-service registrations.
 */
export const createAdminStartupMutation = (
  options?: Partial<Options<CreateAdminStartupData>>,
): UseMutationOptions<
  CreateAdminStartupResponse2,
  CreateAdminStartupError,
  Options<CreateAdminStartupData>
> => {
  const mutationOptions: UseMutationOptions<
    CreateAdminStartupResponse2,
    CreateAdminStartupError,
    Options<CreateAdminStartupData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await createAdminStartup({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

export const listInstagramVerificationsQueryKey = (
  options?: Options<ListInstagramVerificationsData>,
) => createQueryKey("listInstagramVerifications", options);

/**
 * List Instagram verifications for the admin console
 */
export const listInstagramVerificationsOptions = (
  options?: Options<ListInstagramVerificationsData>,
) =>
  queryOptions<
    ListInstagramVerificationsResponse,
    ListInstagramVerificationsError,
    ListInstagramVerificationsResponse,
    ReturnType<typeof listInstagramVerificationsQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await listInstagramVerifications({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: listInstagramVerificationsQueryKey(options),
  });

/**
 * Confirm a startup's Instagram after matching the DM (admin only)
 */
export const confirmInstagramVerificationMutation = (
  options?: Partial<Options<ConfirmInstagramVerificationData>>,
): UseMutationOptions<
  ConfirmInstagramVerificationResponse,
  ConfirmInstagramVerificationError,
  Options<ConfirmInstagramVerificationData>
> => {
  const mutationOptions: UseMutationOptions<
    ConfirmInstagramVerificationResponse,
    ConfirmInstagramVerificationError,
    Options<ConfirmInstagramVerificationData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await confirmInstagramVerification({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};

/**
 * Reset a verification, unlocking the handle (admin only)
 */
export const resetInstagramVerificationMutation = (
  options?: Partial<Options<ResetInstagramVerificationData>>,
): UseMutationOptions<
  ResetInstagramVerificationResponse,
  ResetInstagramVerificationError,
  Options<ResetInstagramVerificationData>
> => {
  const mutationOptions: UseMutationOptions<
    ResetInstagramVerificationResponse,
    ResetInstagramVerificationError,
    Options<ResetInstagramVerificationData>
  > = {
    mutationFn: async (fnOptions) => {
      const { data } = await resetInstagramVerification({
        ...options,
        ...fnOptions,
        throwOnError: true,
      });
      return data;
    },
  };
  return mutationOptions;
};
